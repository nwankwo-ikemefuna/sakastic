jQuery(document).ready(function ($) {
    "use strict";  

    /*//floating login
    $(document).on('submit', '#flogin_form', function(e) {
    	e.preventDefault();
      	var callback = function(jres) {
			if (jres.status) {
			  	$(location).reload();
			}
		}
		var form_data = new FormData(this);
    	var url = $(this).attr('action');
      	post_data_ajax(url, form_data, true, callback, null, true);
    });*/

}); 